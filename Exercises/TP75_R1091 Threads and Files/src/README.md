#### Open the Terminal at the same directory and type "make clean && make"
#### Then execute the project typing "../bin/result.out 5"

**'5'** is the amount of lines that will insert in the "Test.txt"
